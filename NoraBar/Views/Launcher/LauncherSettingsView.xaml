<UserControl x:Class="NoraBar.Views.Launcher.LauncherSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#0078D4" />

        <Style x:Key="SettingCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#12FFFFFF" />
            <Setter Property="BorderBrush" Value="#20FFFFFF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="18,16" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>

        <Style x:Key="CardHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Yu Gothic UI, Meiryo" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#F5F5F5" />
        </Style>

        <Style x:Key="CardSubHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Yu Gothic UI, Meiryo" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#8A8A8A" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>

        <Style x:Key="SettingSquareButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Brd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#35FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI, Yu Gothic UI, Meiryo"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Brd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#25FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#35FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Brd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#005A9E"/>
                                <Setter TargetName="Brd" Property="BorderBrush" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid x:Name="TemplateRoot" Background="Transparent" Cursor="Hand">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="SwitchArea" Grid.Column="0" Width="44" Height="20" Background="#33FFFFFF" CornerRadius="10" BorderBrush="#20FFFFFF" BorderThickness="1">
                                <Ellipse x:Name="Thumb" Width="12" Height="12" Fill="#E3E3E3" HorizontalAlignment="Left" Margin="3,0,3,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform X="0"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchArea" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="SwitchArea" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="24" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction><CircleEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction><CircleEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#0AFFFFFF"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Brd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="#15FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Brd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="0,4,0,0">
        <!-- Hero Card: Visual Editor Launcher -->
        <Border Style="{StaticResource SettingCardStyle}" Background="#1E0078D4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Strings.Launcher}" Style="{StaticResource CardHeaderStyle}"/>
                    <TextBlock Text="{Binding Strings.VisualEditorDescription}" Style="{StaticResource CardSubHeaderStyle}"/>
                </StackPanel>

                <Button Grid.Column="1" Content="{Binding Strings.OpenVisualEditor}" Click="OpenEditor_Click" Style="{StaticResource AccentButtonStyle}" Padding="18,10" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <!-- Card 1: Smart Launcher -->
        <Border Style="{StaticResource SettingCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Strings.SmartLauncher}" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,8"/>
                <CheckBox Content="{Binding Strings.EnableSmart}" IsChecked="{Binding SmartEnabled}" Style="{StaticResource ToggleSwitchStyle}"/>
                <TextBlock Text="{Binding Strings.SmartPrivacy}" Style="{StaticResource CardSubHeaderStyle}" Margin="0,10,0,14"/>
                <Button Content="{Binding Strings.ClearUsage}" Click="ClearUsage_Click" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Left"/>
            </StackPanel>
        </Border>

        <!-- Card 2: Automatic Rules -->
        <Border Style="{StaticResource SettingCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Strings.Rules}" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,6"/>
                <TextBlock Text="{Binding Strings.RulesDescription}" Style="{StaticResource CardSubHeaderStyle}" Margin="0,0,0,12"/>

                <ListBox ItemsSource="{Binding Rules}" x:Name="RulesList" Height="160" Margin="0,0,0,12">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TargetPageId}" FontWeight="SemiBold" Foreground="White" FontSize="13"/>
                                    <TextBlock Foreground="#8AFFFFFF" FontSize="11" Margin="0,2,0,0">
                                        <Run Text="{Binding DataContext.Strings.Priority, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                        <Run Text=" "/>
                                        <Run Text="{Binding Priority}"/>
                                    </TextBlock>
                                </StackPanel>
                                <CheckBox Grid.Column="1" IsChecked="{Binding IsEnabled}" IsHitTestVisible="False" Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <WrapPanel>
                    <Button Content="{Binding Strings.Add}" Click="AddRule_Click" Style="{StaticResource AccentButtonStyle}" Padding="14,6" Margin="0,0,6,4"/>
                    <Button Content="{Binding Strings.Edit}" Click="EditRule_Click" Style="{StaticResource ModernButtonStyle}" Padding="14,6" Margin="0,0,6,4"/>
                    <Button Content="{Binding Strings.Delete}" Click="DeleteRule_Click" Style="{StaticResource ModernButtonStyle}" Padding="14,6" Margin="0,0,6,4"/>
                    <Button Content="↑" Click="MoveRuleUp_Click" ToolTip="{Binding Strings.MoveRuleUp}" Style="{StaticResource SettingSquareButtonStyle}" Margin="0,0,6,4"/>
                    <Button Content="↓" Click="MoveRuleDown_Click" ToolTip="{Binding Strings.MoveRuleDown}" Style="{StaticResource SettingSquareButtonStyle}" Margin="0,0,6,4"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Card 3: Global Shortcuts -->
        <Border Style="{StaticResource SettingCardStyle}">
            <StackPanel>
                <TextBlock Text="{Binding Strings.Shortcuts}" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,10"/>

                <TextBlock Text="{Binding Strings.OpenLauncher}" Style="{StaticResource CardSubHeaderStyle}" Foreground="#D0D0D0" Margin="0,0,0,4"/>
                <TextBox Text="{Binding OpenShortcutText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" ToolTip="{Binding Strings.ShortcutHint}"/>

                <TextBlock Text="{Binding Strings.OpenSearch}" Style="{StaticResource CardSubHeaderStyle}" Foreground="#D0D0D0" Margin="0,0,0,4"/>
                <TextBox Text="{Binding SearchShortcutText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" ToolTip="{Binding Strings.SearchShortcutHint}"/>

                <TextBlock Text="{Binding ShortcutError}" Foreground="#FFFF8A80" TextWrapping="Wrap" Margin="0,0,0,10"/>

                <Button Content="{Binding Strings.ApplyShortcuts}" Click="ApplyShortcuts_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Left" Padding="16,7"/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
