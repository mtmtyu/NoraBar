<UserControl x:Class="NoraBar.Views.Island.DesignA_Minimal.DesignAMusicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:NoraBar.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="106" d:DesignWidth="450">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Border Style="{StaticResource IslandBorderStyle}" Padding="16,14,16,14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Main Contents Row (Row 0) -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Album Art -->
                <Grid Grid.Column="0" Width="48" Height="48" Margin="0,0,16,0" Tag="{Binding Music.Title, NotifyOnTargetUpdated=True}">
                    <Grid.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1" CenterX="24" CenterY="24" />
                    </Grid.RenderTransform>
                    <Grid.Triggers>
                        <EventTrigger RoutedEvent="Binding.TargetUpdated">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.5" To="1" Duration="0:0:0.3" />
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.8" To="1" Duration="0:0:0.4">
                                        <DoubleAnimation.EasingFunction>
                                            <BackEase EasingMode="EaseOut" Amplitude="0.5" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.8" To="1" Duration="0:0:0.4">
                                        <DoubleAnimation.EasingFunction>
                                            <BackEase EasingMode="EaseOut" Amplitude="0.5" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Grid.Triggers>

                    <Border Background="#20FFFFFF" CornerRadius="8" />
                    <TextBlock Text="&#xE8D6;" FontFamily="{StaticResource DefaultFontFamily}" FontSize="20" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Border CornerRadius="8">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding Music.AlbumArt}" Stretch="UniformToFill" />
                        </Border.Background>
                    </Border>
                </Grid>

                <!-- Metadata & Waveform -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <StackPanel.RenderTransform>
                        <TranslateTransform Y="0"/>
                    </StackPanel.RenderTransform>
                    <StackPanel.Triggers>
                        <EventTrigger RoutedEvent="Binding.TargetUpdated">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" />
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="10" To="0" Duration="0:0:0.4">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </StackPanel.Triggers>
                    <TextBlock Text="{Binding Music.Title, NotifyOnTargetUpdated=True}" Style="{StaticResource TitleText}" Margin="0,0,0,2" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding Music.Artist}" Style="{StaticResource CaptionText}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                
                <controls:WaveformVisualizer Grid.Column="2" Margin="16,0,16,0" SpectrumData="{Binding Music.SpectrumData}" BarColor="{StaticResource AccentBrush}" VerticalAlignment="Center" />

                <!-- Controls -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <controls:IconButton IconText="&#xE892;" Margin="0,0,8,0" Command="{Binding Music.PreviousCommand}" /> <!-- Previous -->
                    <controls:IconButton IconText="{Binding Music.IsPlaying, Converter={StaticResource PlayPauseIconConverter}}" Margin="0,0,8,0" Command="{Binding Music.PlayPauseCommand}" /> <!-- Play/Pause -->
                    <controls:IconButton IconText="&#xE893;" Command="{Binding Music.NextCommand}" /> <!-- Next -->
                </StackPanel>
            </Grid>

            <!-- Slim Progress Bar Row (Row 1, independent of Lyrics) -->
            <Grid Grid.Row="1" Margin="0,8,0,0" Visibility="{Binding ShowProgressBar, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Music.PositionText}" Style="{StaticResource CaptionText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <ProgressBar Grid.Column="1" 
                             Minimum="0" Maximum="100" 
                             Value="{Binding Music.ProgressValue}" 
                             Height="3" 
                             Foreground="{StaticResource AccentBrush}" 
                             Background="#1AFFFFFF" 
                             BorderThickness="0" 
                             VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Music.DurationText}" Style="{StaticResource CaptionText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </Grid>

            <!-- Lyrics Row (Row 2, independent of ProgressBar) -->
            <TextBlock Grid.Row="2"
                       Margin="0,8,0,0"
                       Height="18"
                       Text="{Binding Music.CurrentLyric, NotifyOnTargetUpdated=True}"
                       Foreground="#E0FFFFFF"
                       FontSize="13"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       Visibility="{Binding ShowLyrics, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock.RenderTransform>
                    <TranslateTransform Y="0"/>
                </TextBlock.RenderTransform>
                <TextBlock.Triggers>
                    <EventTrigger RoutedEvent="Binding.TargetUpdated">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3" />
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="5" To="0" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut" />
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </TextBlock.Triggers>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
