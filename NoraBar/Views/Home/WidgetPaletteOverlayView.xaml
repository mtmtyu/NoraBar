<UserControl x:Class="NoraBar.Views.Home.WidgetPaletteOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:homeViews="clr-namespace:NoraBar.Views.Home">
    <Border Background="#F514141A" BorderBrush="#40FFFFFF" BorderThickness="1" CornerRadius="16" Padding="16,12" Width="960">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="8" Opacity="0.55" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Top Header -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE710;" FontFamily="Segoe Fluent Icons" Foreground="#FF64B5F6" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding AddWidgetsHeaderText}" FontWeight="Bold" FontSize="14" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding DragToArrangeHintText}" FontSize="12" Foreground="#A0FFFFFF" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Row 1: Middle Widget Miniature Catalog Previews -->
            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,14">
                <ItemsControl x:Name="CatalogItemsControl" ItemsSource="{Binding CatalogWidgets}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <homeViews:WidgetCatalogItemView
                                PreviewMouseLeftButtonDown="CatalogItem_PreviewMouseLeftButtonDown"
                                PreviewMouseMove="CatalogItem_PreviewMouseMove"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Row 2: Bottom Area Controls (Size Sliders & Done Button) -->
            <Grid Grid.Row="2" Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Max Width Slider -->
                <StackPanel Grid.Column="0">
                    <Grid Margin="0,0,0,2">
                        <TextBlock Text="{Binding MaxWidgetWidthLabelText}" FontSize="11" Foreground="#A0FFFFFF" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding MaxWidgetWidth, StringFormat={}{0:F0} px}" FontSize="11" Foreground="#FF64B5F6" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Minimum="300" Maximum="1400" TickFrequency="10" IsSnapToTickEnabled="True" Value="{Binding MaxWidgetWidth, Mode=TwoWay}"/>
                </StackPanel>

                <!-- Max Height Slider -->
                <StackPanel Grid.Column="2">
                    <Grid Margin="0,0,0,2">
                        <TextBlock Text="{Binding MaxWidgetHeightLabelText}" FontSize="11" Foreground="#A0FFFFFF" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding MaxWidgetHeight, StringFormat={}{0:F0} px}" FontSize="11" Foreground="#FF64B5F6" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Minimum="100" Maximum="800" TickFrequency="10" IsSnapToTickEnabled="True" Value="{Binding MaxWidgetHeight, Mode=TwoWay}"/>
                </StackPanel>

                <!-- Done Button at Bottom Right -->
                <Button Grid.Column="4" Content="{Binding DoneText}" Click="DoneButton_Click" VerticalAlignment="Center"
                        Padding="24,8" Background="#FF64B5F6" Foreground="Black" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
