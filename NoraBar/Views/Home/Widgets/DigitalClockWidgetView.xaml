<UserControl x:Class="NoraBar.Views.Home.Widgets.DigitalClockWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Minimal Style Template -->
        <DataTemplate x:Key="ClockMinimalTemplate">
            <StackPanel VerticalAlignment="Center" Margin="10,2">
                <TextBlock Text="{Binding LocalTimeText}" Foreground="#FFFFFFFF" FontSize="19" FontWeight="SemiBold" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Display, Segoe UI">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.3" Color="Black"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="{Binding LocalDateText}" Foreground="#99FFFFFF" FontSize="11" FontWeight="Regular" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Text, Segoe UI"/>
            </StackPanel>
        </DataTemplate>

        <!-- Expressive Style Template -->
        <DataTemplate x:Key="ClockExpressiveTemplate">
            <Border Background="#20FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="10" Padding="14,6" Margin="4,2">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding LocalTimeText}" Foreground="#FF64B5F6" FontSize="21" FontWeight="Bold" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Display, Segoe UI">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.4" Color="#FF64B5F6"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="{Binding LocalDateText}" Foreground="#E0FFFFFF" FontSize="11" FontWeight="Medium" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Text, Segoe UI"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- Bold Gradient Style Template -->
        <DataTemplate x:Key="ClockBoldGradientTemplate">
            <Border CornerRadius="10" BorderBrush="#40FFFFFF" BorderThickness="1" Padding="14,6" Margin="4,2">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#40FF8A65" Offset="0.0"/>
                        <GradientStop Color="#40BA68C8" Offset="1.0"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding LocalTimeText}" Foreground="#FFFFD54F" FontSize="22" FontWeight="Black" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Display, Segoe UI">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5" Color="#FFFFD54F"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="{Binding LocalDateText}" Foreground="#F0FFFFFF" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Text, Segoe UI"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="ClockContentControl" Content="{Binding}"/>
</UserControl>
