<UserControl x:Class="NoraBar.Views.Home.Widgets.DigitalClockWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Minimal Style Template -->
        <DataTemplate x:Key="ClockMinimalTemplate">
            <StackPanel VerticalAlignment="Center" Margin="8,0">
                <TextBlock Text="{Binding LocalTimeText}" Foreground="White" FontSize="18" FontWeight="Medium" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding LocalDateText}" Foreground="#A8FFFFFF" FontSize="11" HorizontalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>

        <!-- Expressive Style Template -->
        <DataTemplate x:Key="ClockExpressiveTemplate">
            <Border Background="#2AFFFFFF" CornerRadius="10" Padding="12,6" Margin="4,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding LocalTimeText}" Foreground="#FF64B5F6" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding LocalDateText}" Foreground="#D0FFFFFF" FontSize="11" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- Bold Gradient Style Template -->
        <DataTemplate x:Key="ClockBoldGradientTemplate">
            <Border CornerRadius="10" Padding="14,6" Margin="4,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#33FF8A65" Offset="0.0"/>
                        <GradientStop Color="#33AB47PT" Offset="1.0"/>
                        <GradientStop Color="#33BA68C8" Offset="1.0"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding LocalTimeText}" Foreground="#FFFFD54F" FontSize="22" FontWeight="Black" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding LocalDateText}" Foreground="#E0FFFFFF" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="ClockContentControl" Content="{Binding}"/>
</UserControl>
