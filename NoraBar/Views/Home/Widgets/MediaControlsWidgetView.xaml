<UserControl x:Class="NoraBar.Views.Home.Widgets.MediaControlsWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:NoraBar.Views.Controls">
    <UserControl.Resources>
        <!-- Compact Style Template -->
        <DataTemplate x:Key="MediaCompactTemplate">
            <Grid Margin="4,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Width="34" Height="34" Margin="0,0,8,0">
                    <Border Background="#2AFFFFFF" CornerRadius="6"/>
                    <TextBlock Text="&#xE93C;" FontFamily="Segoe Fluent Icons" Foreground="#88FFFFFF"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Image Source="{Binding Music.AlbumArt}" Stretch="UniformToFill"/>
                </Grid>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding MediaTitle}" Foreground="White" FontWeight="SemiBold"
                               FontSize="13" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding MediaArtist}" Foreground="#A8FFFFFF" FontSize="11"
                               MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,0"
                            VerticalAlignment="Center" IsEnabled="{Binding HasMedia}">
                    <controls:IconButton IconText="{Binding Music.IsPlaying, Converter={StaticResource PlayPauseIconConverter}}"
                                         Width="28" Height="28"
                                         Command="{Binding Music.PlayPauseCommand}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!-- Expanded Style Template -->
        <DataTemplate x:Key="MediaExpandedTemplate">
            <Border Background="#25FFFFFF" CornerRadius="10" Padding="10,6" Margin="4,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid Width="40" Height="40" Margin="0,0,10,0">
                        <Border Background="#3AFFFFFF" CornerRadius="8"/>
                        <TextBlock Text="&#xE93C;" FontFamily="Segoe Fluent Icons" Foreground="#AAFFFFFF"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Image Source="{Binding Music.AlbumArt}" Stretch="UniformToFill"/>
                    </Grid>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding MediaTitle}" Foreground="White" FontWeight="Bold"
                                   FontSize="13" MaxWidth="180" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding MediaArtist}" Foreground="#C0FFFFFF" FontSize="11"
                                   MaxWidth="180" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0"
                                VerticalAlignment="Center" IsEnabled="{Binding HasMedia}">
                        <controls:IconButton IconText="&#xE892;" Width="26" Height="26"
                                             Command="{Binding Music.PreviousCommand}"/>
                        <controls:IconButton IconText="{Binding Music.IsPlaying, Converter={StaticResource PlayPauseIconConverter}}"
                                             Width="30" Height="30" Margin="4,0"
                                             Command="{Binding Music.PlayPauseCommand}"/>
                        <controls:IconButton IconText="&#xE893;" Width="26" Height="26"
                                             Command="{Binding Music.NextCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="MediaContentControl" Content="{Binding}"/>
</UserControl>
