<UserControl x:Class="NoraBar.Views.Home.Widgets.WorldClockWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Compact Single Style -->
        <DataTemplate x:Key="WorldClockCompactTemplate">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,2">
                <Border Background="#2581D4FA" CornerRadius="4" Padding="6,2" Margin="0,0,8,0">
                    <TextBlock Text="{Binding FirstWorldClockLabel}" Foreground="#FF81D4FA" FontSize="10" FontWeight="Bold"/>
                </Border>
                <TextBlock Text="{Binding FirstWorldClockTimeText}" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" FontFamily="Segoe UI Variable Text, Segoe UI"/>
            </StackPanel>
        </DataTemplate>

        <!-- Dual Card Style -->
        <DataTemplate x:Key="WorldClockDualCardTemplate">
            <Border Background="#1CFFFFFF" BorderBrush="#2AFFFFFF" BorderThickness="1" CornerRadius="10" Padding="12,6" Margin="4,2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,10,0" VerticalAlignment="Center">
                        <Border Background="#2581D4FA" CornerRadius="4" Padding="4,1" HorizontalAlignment="Left" Margin="0,0,0,2">
                            <TextBlock Text="{Binding FirstWorldClockLabel}" Foreground="#FF81D4FA" FontSize="9" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Text="{Binding FirstWorldClockTimeText}" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Border BorderBrush="#30FFFFFF" BorderThickness="1,0,0,0" Margin="0,2,10,2"/>
                    <StackPanel VerticalAlignment="Center">
                        <Border Background="#25CE93D8" CornerRadius="4" Padding="4,1" HorizontalAlignment="Left" Margin="0,0,0,2">
                            <TextBlock Text="{Binding SecondWorldClockLabel}" Foreground="#FFCE93D8" FontSize="9" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Text="{Binding SecondWorldClockTimeText}" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="WorldClockContentControl" Content="{Binding}"/>
</UserControl>
