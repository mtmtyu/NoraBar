<UserControl x:Class="NoraBar.Views.Home.Widgets.WorldClockWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- World Clock Minimal Template (Horizontal Borderless) -->
        <DataTemplate x:Key="WorldClockMinimalTemplate">
            <ItemsControl ItemsSource="{Binding WorldClockItems}" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,2">
                            <TextBlock Text="{Binding Label}" Foreground="#B3FFFFFF" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Display, Segoe UI" Margin="0,0,0,1"/>
                            <TextBlock Text="{Binding TimeText}" Foreground="#FFFFFFFF" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Display, Segoe UI">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.3" Color="Black"/>
                                </TextBlock.Effect>
                            </TextBlock>
                            <TextBlock Text="{Binding DateText}" Foreground="#88FFFFFF" FontSize="10" FontWeight="Regular" HorizontalAlignment="Center" FontFamily="Segoe UI Variable Text, Segoe UI"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>

        <!-- World Clock List Template (Vertical Stacked with Separator Lines) -->
        <DataTemplate x:Key="WorldClockListTemplate">
            <ItemsControl ItemsSource="{Binding WorldClockItems}" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Grid Margin="12,3" MinWidth="180">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Label}" Foreground="#B3FFFFFF" FontSize="12" FontWeight="Bold" FontFamily="Segoe UI Variable Display, Segoe UI" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding TimeText}" Foreground="#FFFFFFFF" FontSize="16" FontWeight="Bold" HorizontalAlignment="Right" FontFamily="Segoe UI Variable Display, Segoe UI">
                                        <TextBlock.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.3" Color="Black"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                    <TextBlock Text="{Binding DateText}" Foreground="#88FFFFFF" FontSize="10" FontWeight="Regular" HorizontalAlignment="Right" FontFamily="Segoe UI Variable Text, Segoe UI"/>
                                </StackPanel>
                            </Grid>
                            <Rectangle Height="1" Fill="#20FFFFFF" Margin="8,2" SnapsToDevicePixels="True"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="ClockContentControl" Content="{Binding}"/>
</UserControl>
